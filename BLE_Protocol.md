# ESP32_BMS_PRO 蓝牙通信协议

## 基本信息

| 项目 | 值 |
|---|---|
| 设备名 | `ESP32_BMS_PRO` |
| Service UUID | `0000ffe0-0000-1000-8000-00805f9b34fb` |
| TX Characteristic (Notify) | `0000ffe1-0000-1000-8000-00805f9b34fb` |
| RX Characteristic (Write) | `0000ffe2-0000-1000-8000-00805f9b34fb` |

- 手机通过 **RX** 写入指令
- ESP32 通过 **TX** Notify 上报数据

---

## 上行帧（ESP32 → 手机）

### 帧格式（共 24 字节）

| 偏移 | 长度 | 字段 | 类型 | 说明 |
|---|---|---|---|---|
| 0 | 1 | 帧头1 | uint8 | 固定 `0xAA` |
| 1 | 1 | 帧头2 | uint8 | 固定 `0x55` |
| 2 | 1 | 长度 | uint8 | 固定 `0x10` (16) |
| 3 | 1 | Status | uint8 | 状态字节（见下方） |
| 4 | 2 | 电压 | uint16 BE | 单位 mV，如 3787 |
| 6 | 2 | 电流 | int16 BE | 单位 mA，正值充电/负值放电 |
| 8 | 1 | 温度 | uint8 | 单位 °C，0 表示无效 |
| 9 | 1 | SOC | uint8 | 百分比 0-100 |
| 10 | 2 | 剩余容量 | uint16 BE | 单位 mAh |
| 12 | 1 | SOH | uint8 | 百分比 0-100 |
| 13 | 2 | 循环次数 | uint16 BE | |
| 15 | 2 | TTE | uint16 BE | 放空剩余时间（分钟），65535=无效 |
| 17 | 2 | TTF | uint16 BE | 充满剩余时间（分钟），65535=无效 |
| 19 | 2 | 充电截止电压 | uint16 BE | 单位 mV，默认 4200 |
| 21 | 2 | 放电截止电压 | uint16 BE | 单位 mV，默认 3000 |
| 23 | 1 | 校验 | uint8 | XOR，byte[2] 到 byte[22] 逐字节异或 |

> BE = Big-Endian，高字节在前。

### Status 字节位定义

| Bit | 含义 | 说明 |
|---|---|---|
| 0 | 充放电模式 | 0=放电，1=充电 |
| 1 | 档位 | 0=1档，1=2档 |
| 2-3 | 运行状态 | 0=停止，1=暂停，2=运行 |
| 4 | SOC 模式 | 0=普通(BQ27220)，1=AI预测 |
| 5-7 | 保留 | |

**Status 解析示例：**

```
bit4  bit3-2  bit1  bit0
  0     10     0     1   = 0x25 → AI模式 + 运行 + 1档 + 充电
```

---

## 下行指令（手机 → ESP32）

手机通过 RX Characteristic 写入字节数组。

### 指令总览

| 指令码 | 名称 | 数据格式 | 说明 |
|---|---|---|---|
| `0x01` | 读取数据 | `[01]` | 立即回复一帧完整数据 |
| `0x02` | 切换模式 | `[02, d0]` | d0=0 放电，d0≠0 充电 |
| `0x03` | 切换档位 | `[03, d0]` | d0=1 一档，d0=2 二档 |
| `0x04` | 运行控制 | `[04, d0]` | d0=0 停止，1 暂停，2 运行 |
| `0x05` | 充电截止 | `[05, hi, lo]` | 截止电压 mV，范围 (放电截止, 4200] |
| `0x06` | 放电截止 | `[06, hi, lo]` | 截止电压 mV，范围 [3000, 充电截止) |
| `0x07` | AI模式切换 | `[07]` | 切换 AI/普通 SOC 模式 |

### 指令详情

#### 0x01 读取数据

- 发送: `[01]`
- 响应: ESP32 立即通过 Notify 回复一帧 24 字节数据
- 无需主循环处理，在 BLE 回调中直接回复

#### 0x02 切换充放电模式

- 发送: `[02, d0]`
- 条件: 仅在停止状态下可切换
- d0=0: 放电模式
- d0≠0: 充电模式

#### 0x03 切换档位

- 发送: `[03, d0]`
- d0=1: 一档
- d0=2: 二档
- 根据当前模式自动切换对应模式的档位

#### 0x04 运行控制

- 发送: `[04, d0]`
- d0=0: 停止
- d0=1: 暂停（仅运行中有效）
- d0=2: 运行（停止或暂停状态下有效）

#### 0x05 充电截止电压

- 发送: `[05, hi, lo]`
- 电压值 mV，big-endian
- 范围: 大于放电截止 且 ≤ 4200
- 例: 设置 4150mV → `[05, 0x10, 0x36]`

#### 0x06 放电截止电压

- 发送: `[06, hi, lo]`
- 电压值 mV，big-endian
- 范围: ≥ 3000 且小于充电截止
- 例: 设置 3000mV → `[06, 0x0B, 0xB8]`

#### 0x07 AI/普通 SOC 模式切换（新增）

- 发送: `[07]`
- 效果: 在 AI 预测模式 和 普通 BQ27220 模式 之间切换
- 条件: AI 模型必须已加载成功，否则指令无效
- 切换后 ESP32 会通过 Notify 上报新状态（Status 字节 bit4 变化）

---

## 数据上报触发方式

| 方式 | 说明 |
|---|---|
| 手动查询 | 发送 `0x01`，立即回复 |
| 定时上报 | ESP32 每秒自动 Notify 一帧 |
| 状态变化 | 指令执行后自动上报 |

---

## 校验算法

```python
def calc_checksum(frame: bytes) -> int:
    """frame 为完整 24 字节帧，校验范围 byte[2] ~ byte[22]"""
    checksum = 0
    for i in range(2, 23):
        checksum ^= frame[i]
    return checksum
```

---

## 更新记录

| 日期 | 变更 |
|---|---|
| 2025-06-01 | 新增 `0x07` AI/普通 SOC 模式切换指令 |
| 2025-06-01 | Status 字节 bit4 新增 AI 模式标志 |
